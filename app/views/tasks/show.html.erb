<h2><%= @task.title %></h2>

<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<br>
<br>
<br>
<p><%= link_to "Edit task", task_edit_path %>

<p><%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>

<p><%= link_to "Back to list", tasks_path %>
